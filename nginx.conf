worker_processes auto;

events {
    worker_connections 128;
}
http {
    access_log off;
    gzip off;

    upstream api {
        server api1:8080;
        server api2:8080;
    }

    server {
        listen 9999;

        location / {
            proxy_pass http://api;
        }
    }
}

